---
# Машинно-зависимая половина конфига (design §3.3). Скопируй в .grace/local.md
# (он в .gitignore) и заполни. Одноимённые ключи ПЕРЕКРЫВАЮТ .grace/project.md.
# Сюда — адреса стендов, команды выкатки, id чатов, абсолютные пути вне репозитория.
# Секретов нет и здесь: только имена переменных и путь к .env.
# Отсутствие local.md — не ошибка: прогон идёт, деплой недоступен.
stand:
  url:                               # https://… — адрес стенда, если он есть
  deploy_cmd:                        # команда выкатки (с адресом/хостом)
  rollback_cmd:                      # команда отката
notify: { channel: desktop, chat: }  # desktop | telegram · chat — id чата для telegram
board_url: http://127.0.0.1:4317     # на VPS — свой адрес доски
---

## Локальные пути вне репозитория
- корень проектов: /Users/<user>/Projects
- бинарь claude: /Users/<user>/.local/bin/claude

## Сервисы этой машины
- (пусто = проверял, сервисов под этот проект на машине нет)
