# Снимок скилла `graceboard-plan` — версия v1

Снято 2026-07-28 из `~/.claude/skills/graceboard-plan/` (каталог скиллов не под git,
поэтому копия живёт здесь, в репозитории Доски).

| Файл | Что это |
|---|---|
| `SKILL.md` | v1, 374 строки / 33.5 кБ — та версия, которая прошла холостой прогон (`docs/plans/quota-resilience.md`) |
| `reference.md` | справочник порогов и два разобранных примера, 164 строки |
| `gb.mjs.snapshot` | состояние помощника `~/.claude/bin/gb.mjs` на тот же момент (флаги брифа уже есть, флагов политики релиза ещё нет) |

Предложения по v2 — `docs/PROPOSAL-graceboard-plan-v2.md`.
Восстановление: `cp SKILL.md reference.md ~/.claude/skills/graceboard-plan/`.
